version: 0.0
os: linux
files:
  - source: /
    destination: /tmp/good-list-frontend
permissions:
  - object: /tmp/good-list-frontend
    owner: ubuntu
    group: ubuntu
hooks:
  BeforeInstall:
    - location: before_install.sh
      runas: ubuntu
  AfterInstall:
    - location: after_install.sh
      runas: ubuntu
